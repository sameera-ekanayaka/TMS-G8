# /backend/render.yaml
services:
  - type: web
    name: tms-backend
    env: node
    region: singapore
    buildCommand: npm install && npx prisma generate && npx prisma migrate deploy
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production