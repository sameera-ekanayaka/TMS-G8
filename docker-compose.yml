services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    depends_on:
      db:
        condition: service_healthy
    environment:
      - DATABASE_URL=mysql://root:rootpassword@db:3306/tms_db
    restart: on-failure

  frontend:
    build:
      context: ./frontend
      # Point the built bundle at the backend. Override these for a real
      # deployment (e.g. https://your-api.example.com).
      args:
        VITE_API_URL: http://localhost:5000
        VITE_WS_URL: http://localhost:5000
    ports:
      - "5173:80"
    depends_on:
      - backend

  db:
    image: mysql:8
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: tms_db
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    # Let backend wait until MySQL is actually ready to accept connections,
    # otherwise `prisma migrate deploy` can run before the DB is up.
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-prootpassword"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  mysql_data:
